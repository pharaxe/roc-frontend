import { DraftableDirective } from './draftable.directive';

describe('DraftableDirective', () => {
  it('should create an instance', () => {
    const directive = new DraftableDirective();
    expect(directive).toBeTruthy();
  });
});
